public class SecondInterfaceClass implements StaticInterfaceClass.NiceNested {
    // имплементировали этот класс, а также его интерфейс
    @Override
    //... переопределили метод этого классса
    public boolean isNotTrue(int x) {
        return true;
    }
}
